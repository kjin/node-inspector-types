{
  "name": "node-inspector-types",
  "version": "0.0.1",
  "scripts": {
    "format": "mkdir -p scripts && cp -r src/* scripts && tslint --fix -p .",
    "build": "npm run format && tsc && cp scripts/generate-inspector/*.template build/generator-inspector && node build/generator-inspector"
  },
  "keywords": [],
  "author": "Kelvin Jin (kelvinjin@google.com)",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^8.0.25",
    "typescript": "^2.4.2"
  },
  "dependencies": {
    "devtools-protocol": "0.0.499605",
    "trim-right": "^1.0.1"
  }
}
